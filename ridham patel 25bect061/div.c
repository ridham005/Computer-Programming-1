#include<stdio.h>
int main()
  {
      float a,b, c;
      printf("enter two numbers:");
      scanf("%f %f",&a,&b);
      c = a / b;
      printf("%f / %f = %f\n",a,b,c);
      return 0;
  }
