#include<stdio.h>
int main()
  {
      float a,b, c;
      printf("enter two numbers:");
      scanf("%d %d",&a,&b);
      c = a / b;
      printf("%d / %d = %d\n",a,b,c);
      return 0;
  }
